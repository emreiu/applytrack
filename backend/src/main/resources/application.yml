spring:
  application:
    name: backend
  jpa:
    open-in-view: false

api:
  base-path: /api/v1

app:
  frontend-base-url: http://localhost:4200
  jwt:
    access-token-ttl: 15m
    refresh-token-ttl: 30d
  auth:
    cookie:
      name: refresh_token